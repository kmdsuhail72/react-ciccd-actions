name: Deploy Dist
on: [push,workflow_dispatch]
jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - name: Get Code
              uses: actions/checkout@v6
            - name : Install Node 22
              uses: actions/setup-node@v6
              with:
                node-version: 22
            - name: print node version
              run: node -v
            - name: Install Dependencies
              run: npm ci
            - name: Run Tests
              run : npm run test
            

            #   run : |
            #     echo "cloning repo"
            #     echo "${{toJson(github)}}"
            #     git clone https://github.com/${{github.repository}}.git
# name: CI Pipeline

# on:
#   push:
#     branches: [main, master]
#   pull_request:
#     branches: [main, master]
#   workflow_dispatch:

# jobs:
#   install-and-test:
#     name: Install Dependencies & Run Tests
#     runs-on: ubuntu-latest
    
#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v4
        
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '18.x'
#           cache: 'npm'
          
#       - name: Install All Dependencies
#         run: |
#           npm install react-router-dom axios @mui/material @emotion/react @emotion/styled @mui/icons-material react-markdown
#           npm ci
          
#       - name: Verify Installation
#         run: |
#           echo "Checking installed packages:"
#           npm list react-router-dom @mui/material react-markdown
          
#       - name: Run Tests
#         run: npm test -- --passWithNoTests
#         env:
#           CI: true
          
#       - name: Build Project
#         run: npm run build
        
#       - name: Upload Build Artifact
#         if: success()
#         uses: actions/upload-artifact@v4
#         with:
#           name: build
#           path: build/